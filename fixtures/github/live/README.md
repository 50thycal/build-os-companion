# Live-payload fixtures

Recorded from real GitHub responses for `50thycal/party-games` and `50thycal/build-os` on
2026-08-24, during the Phase 2 live-ingestion validation. Every value here was observed, not
invented; the files exist so the regressions found that day stay found.

Each fixture is named for the assumption it disproves. See `tests/github-live-regression.test.ts`
and `docs/LIVE_SYNC_VALIDATION.md`.
